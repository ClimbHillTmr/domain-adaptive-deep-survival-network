## 清理目标
- 去除历史遗留、未集成或重复的脚本与目录，仅保留当前端到端管线（data_preprocessing → src → scripts → runs、configs）。
- 归档旧方法与模型产物，避免影响当前训练与评估。

## 保留目录
- `src/`、`scripts/`、`configs/`、`data_preprocessing/`、`runs/`、`scalers/`、`.trae/`、`.gitignore`、`requirements.txt`

## 待归档或删除项
- 归档到 `archive/`（建议）：
  - `Methods/`、`Method_Utils/`、`TimeSeries/`、`模型2.1/`、`模型2.2/`、`透前模型/`、`evaluation_results/`、`Results/`
- 删除（可选，若体积压力大且已无依赖）：
  - `Methods/Models/model_checkpoints/` 下旧 ckpt（非当前管线使用）
  - `runs/` 中早期试跑的无阈值/无校准条目，可保留最新一周与含校准/阈值化的条目

## 冲突与依赖检查
- 检查仓库内是否有文件 import `Methods`/`TimeSeries`，若无则安全归档；当前搜查显示仅旧脚本引用，`src` 管线未依赖。
- 保留 `data_preprocessing/data/*final_data.csv` 与构建脚本 `HBD_data*.py`，其为当前数据来源。

## 执行步骤
1. 创建 `archive/` 目录，将以下文件夹移动至 `archive/`：`Methods/`、`Method_Utils/`、`TimeSeries/`、`模型2.1/`、`模型2.2/`、`透前模型/`、`evaluation_results/`、`Results/`。
2. 在 `runs/` 保留最近的条目与含 `external_metrics_thresholded.json`、`calibration_external_class*.png` 的条目，其余移动到 `archive/runs_old/`。
3. 核验 `scripts/run_phase_classification.py`、`src/*` 无对上述归档目录的依赖；若发现残留引用，再做针对性修复。
4. 输出清理报告（条目数量、占用空间、保留策略）。

## 产出
- `archive/` 目录与 `archive/runs_old/`，保留当前主干所需的最小集；清理报告。

## 风险与回滚
- 所有移动操作均可回滚；删除仅在用户确认后执行。